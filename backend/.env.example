PORT=5000
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority
JWT_SECRET=your_jwt_secret_key

# Firebase Configuration
FIREBASE_PROJECT_ID=your_firebase_project_id
# Base64 encoded service account key (didapatkan dari Firebase Console)
# Untuk mendapatkan service account key:
# 1. Buka Firebase Console > Project Settings > Service Accounts
# 2. Klik "Generate new private key"
# 3. Download file JSON
# 4. Encode file JSON tersebut ke Base64 dan masukkan di sini
FIREBASE_SERVICE_ACCOUNT_KEY=YOUR_BASE64_ENCODED_SERVICE_ACCOUNT_KEY